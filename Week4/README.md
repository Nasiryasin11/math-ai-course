## Week 4: Learning Math with AI

For Week 4, I created a study guide on **Singular Value Decomposition and Low-Rank Approximation** in `Week4/Study-guide.md`. The guide includes a graduate-level concept explanation, worked examples, practice problems with solutions, connections to other mathematical areas, and a refinement process.

I also created a Copilot skill called **Math Tutor** using the VS Code skill creator UI. The UI-created skill is saved in:

```text
.github/skills/math-tutor/SKILL.md
```

I then recreated the same skill manually in:

```text
.agent/skills/math-tutor/skill.md
```

For submission, I copied the contents of the manual `skill.md` file into:

```text
Week4/skill-notes.md
```

I tested the skill by using math-related trigger phrases and the `/math-tutor` slash command. The generated files `Eckart-Young.md`, `GRAD-SVD-Problems.md`, and `GRAD-SVD-Study-Guide.md` were produced during the skill testing process.
